# Dependencies
node_modules/
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# Build / types
dist/
*.tsbuildinfo
# worker-configuration.d.ts is generated by `wrangler types` and committed

# OS / editor
.DS_Store
Icon?
Icon
.idea/
.vscode/
*.swp
*.swo

# Logs / coverage
*.log
coverage/
.nyc_output/

# Env / secrets — never commit real values
# Local Worker secrets: .dev.vars (gitignored)
# Production Worker secrets: `npx wrangler secret put NAME`
# CI: GitHub Actions secrets only (CLOUDFLARE_API_TOKEN, etc.)
.env
.env.*
!.env.example
*.pem
*.p12
credentials.json
secrets.json
secrets.*.json

# Dropbox
.dropbox
.dropbox.attr
