# Copy to .env (gitignored). Never commit real RTSP URLs.

INGEST_TOKEN=dev-ingest-token-change-me
POLJE_API=https://opg-ivanjovic.hr
SNAPSHOT_INTERVAL_SEC=600

# Optional — leave empty for placeholder OFFLINE stills
CAMERA_YARD_RTSP=
CAMERA_GARDEN_RTSP=
CAMERA_HAY_RTSP=
