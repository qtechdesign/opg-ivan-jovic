# Local / non-Worker tooling only.
# Production Worker secrets live in Cloudflare Secrets (wrangler secret put).
# Copy to .env for local edge/MQTT tools later — never commit .env.

# FARM_ID=ivan-jovic
# MQTT_URL=mqtt://mqtt.farm.lan:1883
