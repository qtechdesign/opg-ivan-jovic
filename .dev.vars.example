# Local Wrangler secrets only (never commit this file).
# Production: npx wrangler secret put INGEST_TOKEN
# Later modules also use: XAI_API_KEY, FARM_JWT_SECRET, AGENT_TOKEN

INGEST_TOKEN=dev-ingest-token-change-me
